@model IEnumerable<Cart2.Models.Person>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-responsive-sm">
    <tr>
        <th>
            Customers
        </th>
        <th></th>
    </tr>

    @foreach (var person in Model)
    {
        <tr>
            <td>
                <b>@Html.DisplayFor(modelItem => person.Name)</b> (@Html.DisplayFor(modelItem => person.Number))
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = person.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = person.Id }) |
                @Html.ActionLink("List Items", "Details", new { id = person.Id }) |
            </td>
        </tr>
    }

</table>
